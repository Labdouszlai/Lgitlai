[project]
name = "lgitlai-backend"
version = "1.0.0"
description = "GitHub repository analyzer backend"
authors = [{ name = "labdouszlai" }]
license = { text = "MIT" }
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"
